# Documentation for client/src/components/client-only.tsx

Wrapper component that only renders children on the client side.
 Useful for avoiding hydration mismatches with dynamic data like auth state.
/